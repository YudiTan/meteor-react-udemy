import {Meteor} from 'meteor/meteor';
import {Players} from './../imports/api/players'; //on the server, this uses mongo, as opposed to minimongo on the client, even though they are the same import / function

Meteor.startup(() => {
  
});
